import PlaceholderPage from "../public/placeholder_page";

function Blog({ urls }) {
  return (
    <PlaceholderPage
      description="Articles and updates will be published here soon."
      heading="Blog"
      urls={urls}
    />
  );
}

export default Blog;
