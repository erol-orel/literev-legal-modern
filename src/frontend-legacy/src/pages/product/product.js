import PlaceholderPage from "../public/placeholder_page";

function Product({ urls }) {
  return (
    <PlaceholderPage
      description="Product details will be available here soon."
      heading="Product"
      urls={urls}
    />
  );
}

export default Product;
