import PlaceholderPage from "../public/placeholder_page";

function Company({ urls }) {
  return (
    <PlaceholderPage
      description="Company information will be available here soon."
      heading="Company"
      urls={urls}
    />
  );
}

export default Company;
