# lt-legal

Standalone helpers for French legal sentence splitting and major/minor
extraction workflows.

## Layout

- `src/lt_legal/`: package source code
- `tests/`: package-local test suite
- `pyproject.toml`: standalone package metadata

## Development

Install this package in editable mode from the library root when working on it
independently:

```bash
pip install -e .
pytest
```
