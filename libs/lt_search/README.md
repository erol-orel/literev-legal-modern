# lt-search

Standalone Elasticsearch collector package built on the shared literev contracts
and query helpers.

## Layout

- `src/lt_search/`: package source code
- `tests/`: package-local test suite
- `pyproject.toml`: standalone package metadata

## Development

Install this package in editable mode from the library root when working on it
independently:

```bash
pip install -e .
pytest
```
