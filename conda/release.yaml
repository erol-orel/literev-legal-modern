name: literev-release
channels:
  - nodefaults
  - conda-forge
dependencies:
  - nodejs >=18
  - python 3.11.*
  - pip
  - uv
  - pip:
    - makim==1.20.0
    # fix issues with distlib
    - paginate
    # fix issues with distlib
    - jsbeautifier==1.15.1
