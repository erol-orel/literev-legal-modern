name: literev-legal
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python 3.10.*
  - poetry >=2
  - gunicorn 21.*
  - pip
  - pip:
    - makim==1.20.0
    # fix issues with distlib
    - paginate
    # fix issues with distlib
    - jsbeautifier==1.15.1
