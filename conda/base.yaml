name: literev-legal
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python 3.11.*
  - gunicorn 21.*
  - pip
  - uv
  - pip:
    - makim==1.20.0
    # fix issues with distlib
    - paginate
    # fix issues with distlib
    - jsbeautifier==1.15.1
